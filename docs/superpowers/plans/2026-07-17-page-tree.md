# 페이지 트리 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 스페이스 내 문서를 부모-자식 페이지 트리로 확장 — 하위 문서 생성, 이동(순환 방지), 삭제 시 자식 승격, 사이드바 트리, API/MCP 반영.

**Architecture:** `Page.parentId` self-relation(additive). 트리 구성·순환 검증은 순수 함수(`src/lib/page-tree.ts`)로 분리해 단위 테스트. 이동 로직은 `movePageInSpace`(lib)로 공용화해 서버 액션과 API가 공유. UI 실패는 `?error=` 배너 패턴(PR #8 확립) 재사용.

**Tech Stack:** Prisma self-relation, Next.js 서버 컴포넌트/액션, 클라이언트 트리 렌더(SidebarDocs), MCP SDK.

**Spec:** `docs/superpowers/specs/2026-07-17-page-tree-design.md`

## Global Constraints

- 브랜치 `feat/page-tree` (체크아웃됨, base = PR #8·#9 머지된 main `161e1ab` + 스펙 커밋).
- dev PostgreSQL :5433 (docker). 타입체크 `npx tsc --noEmit`. **`npm run build` 절대 금지** (dev 서버 :3000 — .next 파손).
- **마이그레이션 후에는 dev 서버(:3000)를 재기동해야 함** — 프로세스가 구 Prisma Client를 캐시. 재기동은 컨트롤러 담당(구현자는 요청만).
- 마이그레이션: additive면 `npm run db:migrate -- --name <이름>` 통상 동작. stray `searchVector DROP DEFAULT` 줄 확인·제거. non-interactive 거부 시 diff→수동 폴더→deploy 우회. **DB reset 금지** (Prisma AI 안전장치 — 컨트롤러+사용자 동의 필요).
- 형제 정렬은 제목순(`localeCompare(ko)`), slug·URL·위키링크는 트리와 독립(불변).
- UI 문구 한국어, 기존 클래스 재사용. 커밋: prefix+한국어+`Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- e2e 최종 기대치: **13개** (기존 12 + 신규 1).

---

### Task 1: 스키마 + 트리 순수 로직 (TDD)

**Files:**
- Modify: `prisma/schema.prisma` (Page 모델)
- Create: `src/lib/page-tree.ts`
- Test: `tests/page-tree.test.ts`

**Interfaces:**
- Produces: `Page.parentId: string | null` (+`parent`/`children` relation). `buildTree(pages) → TreeNode[]`(형제 제목순), `selfAndDescendantIds(pages, rootId) → Set<string>`, `flattenTree(roots) → {id,slug,title,depth}[]`. 이후 태스크 전부가 소비.

- [ ] **Step 1: 스키마 — Page에 트리 필드 추가**

`prisma/schema.prisma`의 `Page` 모델에서 `version` 필드 다음에 추가:

```prisma
  // 페이지 트리: 같은 스페이스 안의 부모 문서(null=최상위). 순환·타 스페이스 부모는
  // 앱(lib/pages.ts movePageInSpace, createPageInSpace)에서 검증한다.
  // 부모 삭제 시 자식은 deletePage가 조부모로 승격한다.
  parentId  String?
  parent    Page?   @relation("PageTree", fields: [parentId], references: [id])
  children  Page[]  @relation("PageTree")
```

`@@unique([spaceId, slug])` 아래 인덱스 블록에 추가:

```prisma
  @@index([parentId])
```

- [ ] **Step 2: 마이그레이션**

```bash
npm run db:migrate -- --name add_page_tree
```

Expected: `ALTER TABLE "Page" ADD COLUMN "parentId" TEXT;` + parentId 인덱스 + self-FK만. stray 줄 확인. 적용 후 **컨트롤러에게 dev 서버 재기동을 요청**하고 다음 스텝 진행(단위 테스트는 서버 무관).

- [ ] **Step 3: 실패하는 테스트 작성**

`tests/page-tree.test.ts` 생성:

```ts
import { describe, it, expect } from "vitest";
import { buildTree, selfAndDescendantIds, flattenTree, type TreePageInput } from "@/lib/page-tree";

const pages: TreePageInput[] = [
  { id: "1", slug: "guide", title: "가이드", parentId: null },
  { id: "2", slug: "onboard", title: "온보딩", parentId: "1" },
  { id: "3", slug: "arch", title: "아키텍처", parentId: "1" },
  { id: "4", slug: "faq", title: "FAQ", parentId: null },
  { id: "5", slug: "deep", title: "심화", parentId: "2" },
];

describe("buildTree", () => {
  it("부모-자식 중첩과 형제 제목순 정렬", () => {
    const roots = buildTree(pages);
    expect(roots.map((r) => r.title)).toEqual(["FAQ", "가이드"]);
    const guide = roots[1];
    expect(guide.children.map((c) => c.title)).toEqual(["아키텍처", "온보딩"]);
    expect(guide.children[1].children.map((c) => c.title)).toEqual(["심화"]);
  });
  it("목록에 없는 부모를 가진 문서(고아)는 최상위로", () => {
    const roots = buildTree([{ id: "x", slug: "x", title: "X", parentId: "ghost" }]);
    expect(roots.map((r) => r.title)).toEqual(["X"]);
  });
});

describe("selfAndDescendantIds", () => {
  it("자기 자신 + 모든 자손", () => {
    expect(selfAndDescendantIds(pages, "1")).toEqual(new Set(["1", "2", "3", "5"]));
    expect(selfAndDescendantIds(pages, "4")).toEqual(new Set(["4"]));
  });
});

describe("flattenTree", () => {
  it("들여쓰기 depth와 트리 순서", () => {
    const flat = flattenTree(buildTree(pages));
    expect(flat.map((f) => `${f.depth}:${f.title}`)).toEqual([
      "0:FAQ",
      "0:가이드",
      "1:아키텍처",
      "1:온보딩",
      "2:심화",
    ]);
  });
});
```

- [ ] **Step 4: 실패 확인**

```bash
npm test -- tests/page-tree.test.ts
```

Expected: FAIL — 모듈 없음.

- [ ] **Step 5: page-tree.ts 구현**

`src/lib/page-tree.ts` 생성:

```ts
// 페이지 트리 순수 로직 — DB 무관. 서버(액션·페이지)와 클라이언트(사이드바) 양쪽에서 쓴다.

export interface TreePageInput {
  id: string;
  slug: string;
  title: string;
  parentId: string | null;
}

export interface TreeNode extends TreePageInput {
  children: TreeNode[];
}

// 플랫 목록 → 형제 제목순 트리. parentId가 목록에 없는 문서(이론상 고아)는 최상위로 올린다.
export function buildTree(pages: TreePageInput[]): TreeNode[] {
  const nodes = new Map<string, TreeNode>();
  for (const p of pages) nodes.set(p.id, { ...p, children: [] });
  const roots: TreeNode[] = [];
  for (const node of nodes.values()) {
    const parent = node.parentId ? nodes.get(node.parentId) : undefined;
    if (parent) parent.children.push(node);
    else roots.push(node);
  }
  const sortRec = (list: TreeNode[]) => {
    list.sort((a, b) => a.title.localeCompare(b.title, "ko"));
    for (const n of list) sortRec(n.children);
  };
  sortRec(roots);
  return roots;
}

// rootId 자신 + 모든 자손의 id 집합 — 이동 시 순환 방지용.
export function selfAndDescendantIds(
  pages: { id: string; parentId: string | null }[],
  rootId: string,
): Set<string> {
  const childrenOf = new Map<string, string[]>();
  for (const p of pages) {
    if (!p.parentId) continue;
    const arr = childrenOf.get(p.parentId) ?? [];
    arr.push(p.id);
    childrenOf.set(p.parentId, arr);
  }
  const result = new Set<string>([rootId]);
  const stack = [rootId];
  while (stack.length > 0) {
    const cur = stack.pop()!;
    for (const child of childrenOf.get(cur) ?? []) {
      if (!result.has(child)) {
        result.add(child);
        stack.push(child);
      }
    }
  }
  return result;
}

// 트리 → 들여쓰기 depth를 가진 플랫 목록. 이동 드롭다운·MCP 텍스트 표기용.
export function flattenTree(
  roots: TreeNode[],
  depth = 0,
): { id: string; slug: string; title: string; depth: number }[] {
  const out: { id: string; slug: string; title: string; depth: number }[] = [];
  for (const n of roots) {
    out.push({ id: n.id, slug: n.slug, title: n.title, depth });
    out.push(...flattenTree(n.children, depth + 1));
  }
  return out;
}
```

- [ ] **Step 6: 통과 확인 + 전체 검증**

```bash
npm test && npx tsc --noEmit
```

Expected: page-tree 5개 포함 전부 PASS(57개), 에러 0.

- [ ] **Step 7: Commit**

```bash
git add prisma/schema.prisma prisma/migrations src/lib/page-tree.ts tests/page-tree.test.ts
git commit -m "feat(schema): Page.parentId 트리 필드 + 트리 순수 로직

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: 생성 경로 — 하위 문서

**Files:**
- Modify: `src/lib/pages.ts` (`WritePageInput`, `createPageInSpace`)
- Modify: `src/actions/pages.ts` (`createPage` 시그니처)
- Modify: `src/app/s/[spaceKey]/new/page.tsx`
- Modify: `src/app/s/[spaceKey]/[slug]/page.tsx` (하위 문서 버튼)

**Interfaces:**
- Consumes: Task 1의 `Page.parentId`.
- Produces: `createPage(spaceKey, parentSlug: string | null, formData)` (시그니처 변경 — 호출처는 new 페이지 1곳), `WritePageInput.parentId?: string | null`. e2e 계약 — 문서 화면 버튼 텍스트 "하위 문서".

- [ ] **Step 1: lib — createPageInSpace에 parentId**

`src/lib/pages.ts`의 `WritePageInput`에 필드 추가:

```ts
  // 페이지 트리: 부모 페이지 id(null/생략 = 최상위). 같은 스페이스인지는 호출자가 검증한다.
  parentId?: string | null;
```

`createPageInSpace`의 `tx.page.create` data에 한 줄 추가:

```ts
        parentId: input.parentId ?? null,
```

- [ ] **Step 2: createPage 액션 — parent slug 처리**

`src/actions/pages.ts`의 `createPage`를 다음으로 교체:

```ts
export async function createPage(spaceKey: string, parentSlug: string | null, formData: FormData) {
  const { session, space } = await requireSpaceRole(spaceKey, "editor");
  const title = String(formData.get("title") ?? "").trim();
  const content = String(formData.get("content") ?? "");

  let parentId: string | null = null;
  if (parentSlug) {
    const parent = await prisma.page.findUnique({
      where: { spaceId_slug: { spaceId: space.id, slug: parentSlug } },
      select: { id: true },
    });
    // 부모가 그사이 삭제된 경우 — 조용히 최상위로 만들지 않고 명시적으로 알린다.
    if (!parent) {
      redirect(`/s/${spaceKey}/new?error=${encodeURIComponent("부모 문서가 없습니다. 삭제되었을 수 있습니다.")}`);
    }
    parentId = parent.id;
  }

  const { slug, created } = await createPageInSpace({
    spaceId: space.id,
    title,
    content,
    authorId: session.userId,
    parentId,
  });
  // 같은 slug가 이미 있으면 그 페이지의 편집 화면으로 보낸다.
  if (!created) redirect(`/s/${spaceKey}/${encodeURIComponent(slug)}/edit`);

  revalidatePath(`/s/${spaceKey}`);
  redirect(`/s/${spaceKey}/${encodeURIComponent(slug)}`);
}
```

- [ ] **Step 3: new 페이지 — parent 안내 + 에러 배너**

`src/app/s/[spaceKey]/new/page.tsx` 전체 교체:

```tsx
import { requireSpaceRole } from "@/lib/access";
import { prisma } from "@/lib/db";
import { createPage } from "@/actions/pages";
import { MarkdownEditor } from "@/components/MarkdownEditor";

export default async function NewPagePage({
  params,
  searchParams,
}: {
  params: Promise<{ spaceKey: string }>;
  searchParams: Promise<{ title?: string; parent?: string; error?: string }>;
}) {
  const { spaceKey } = await params;
  const { title, parent, error } = await searchParams;
  const { space } = await requireSpaceRole(spaceKey, "editor");

  // 부모 문서 확인 — 없으면(잘못된 링크 등) 최상위 생성으로 조용히 폴백하지 않고 안내만 남긴다.
  const parentPage = parent
    ? await prisma.page.findUnique({
        where: { spaceId_slug: { spaceId: space.id, slug: parent } },
        select: { slug: true, title: true },
      })
    : null;

  return (
    <main className="py-10">
      <p className="eyebrow">{spaceKey} · new page</p>
      <h1 className="page-title mb-5 mt-1">새 페이지</h1>
      {error && (
        <div className="notice notice-warn mb-4" role="alert">
          {error}
        </div>
      )}
      {parentPage && (
        <p className="meta mb-4">&apos;{parentPage.title}&apos; 하위에 만듭니다.</p>
      )}
      <MarkdownEditor
        spaceKey={spaceKey}
        initialTitle={title ?? ""}
        initialContent=""
        onSave={createPage.bind(null, spaceKey, parentPage?.slug ?? null)}
      />
    </main>
  );
}
```

- [ ] **Step 4: 문서 화면 — "하위 문서" 버튼**

`src/app/s/[spaceKey]/[slug]/page.tsx`의 canEdit 버튼 그룹에서 "편집" 링크 앞에 추가:

```tsx
            <Link
              href={`/s/${spaceKey}/new?parent=${encodeURIComponent(slug)}`}
              className="btn btn-ghost btn-sm"
            >
              하위 문서
            </Link>
```

- [ ] **Step 5: 검증 + Commit**

```bash
npm test && npx tsc --noEmit
git add src/lib/pages.ts src/actions/pages.ts "src/app/s/[spaceKey]/new/page.tsx" "src/app/s/[spaceKey]/[slug]/page.tsx"
git commit -m "feat(tree): 하위 문서 생성 — 문서 화면 버튼 + 부모 고정 생성

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: 이동 + 삭제 시 자식 승격

**Files:**
- Modify: `src/lib/pages.ts` (`movePageInSpace` 신규)
- Modify: `src/actions/pages.ts` (`movePage` 신규, `deletePage` 승격)
- Modify: `src/app/s/[spaceKey]/[slug]/page.tsx` (이동 폼 + 에러 배너)

**Interfaces:**
- Consumes: Task 1의 `selfAndDescendantIds`/`buildTree`/`flattenTree`.
- Produces: `movePageInSpace({ spaceId, slug, parentSlug }) → { ok: true } | { ok: false; reason: "not-found" | "parent-not-found" | "cycle" }` — Task 5의 API도 소비. e2e 계약 — 문서 화면 `select[name="parent"]` + 버튼 "이동".

- [ ] **Step 1: lib — movePageInSpace**

`src/lib/pages.ts`에 import 추가(`import { selfAndDescendantIds } from "@/lib/page-tree";`) 후 파일 하단에 추가:

```ts
export type MovePageResult =
  | { ok: true }
  | { ok: false; reason: "not-found" | "parent-not-found" | "cycle" };

/**
 * 페이지를 트리에서 이동한다(parentSlug=null이면 최상위). 본문 불변 — 리비전을 만들지 않는다.
 * 자기 자신·자손으로의 이동(순환)과 타 스페이스 부모는 거부한다.
 */
export async function movePageInSpace(input: {
  spaceId: string;
  slug: string;
  parentSlug: string | null;
}): Promise<MovePageResult> {
  const pages = await prisma.page.findMany({
    where: { spaceId: input.spaceId },
    select: { id: true, slug: true, parentId: true },
  });
  const page = pages.find((p) => p.slug === input.slug);
  if (!page) return { ok: false, reason: "not-found" };

  let parentId: string | null = null;
  if (input.parentSlug !== null) {
    const parent = pages.find((p) => p.slug === input.parentSlug);
    if (!parent) return { ok: false, reason: "parent-not-found" };
    if (selfAndDescendantIds(pages, page.id).has(parent.id)) return { ok: false, reason: "cycle" };
    parentId = parent.id;
  }
  await prisma.page.update({ where: { id: page.id }, data: { parentId } });
  return { ok: true };
}
```

- [ ] **Step 2: movePage 액션 + deletePage 승격**

`src/actions/pages.ts` import에 `movePageInSpace` 추가, 다음 액션 추가:

```ts
export async function movePage(spaceKey: string, slug: string, formData: FormData) {
  const { space } = await requireSpaceRole(spaceKey, "editor");
  const raw = String(formData.get("parent") ?? "");
  const result = await movePageInSpace({
    spaceId: space.id,
    slug,
    parentSlug: raw === "" ? null : raw,
  });
  if (!result.ok) {
    const msg =
      result.reason === "cycle"
        ? "자기 자신이나 하위 문서로는 이동할 수 없습니다."
        : result.reason === "parent-not-found"
          ? "대상 문서가 없습니다. 삭제되었을 수 있습니다."
          : "문서가 없습니다.";
    console.warn(`[tree] 이동 실패 — ${result.reason} (space=${spaceKey}, slug=${JSON.stringify(slug)})`);
    redirect(`/s/${spaceKey}/${encodeURIComponent(slug)}?error=${encodeURIComponent(msg)}`);
  }
  revalidatePath(`/s/${spaceKey}`);
  revalidatePath(`/s/${spaceKey}/${encodeURIComponent(slug)}`);
  redirect(`/s/${spaceKey}/${encodeURIComponent(slug)}`);
}
```

`deletePage`를 다음으로 교체 (자식 승격):

```ts
export async function deletePage(spaceKey: string, slug: string) {
  const { space } = await requireSpaceRole(spaceKey, "editor");
  const page = await prisma.page.findUnique({
    where: { spaceId_slug: { spaceId: space.id, slug } },
    select: { id: true, parentId: true },
  });
  if (page) {
    // 자식은 삭제 문서의 부모로 승격 — 하위 내용이 함께 사라지지 않는다.
    await prisma.$transaction([
      prisma.page.updateMany({ where: { parentId: page.id }, data: { parentId: page.parentId } }),
      prisma.page.delete({ where: { id: page.id } }),
    ]);
    invalidatePageCache(page.id);
  }
  revalidatePath(`/s/${spaceKey}`);
  redirect(`/s/${spaceKey}`);
}
```

- [ ] **Step 3: 문서 화면 — 이동 폼 + 에러 배너**

`src/app/s/[spaceKey]/[slug]/page.tsx`:

1. import에 추가: `import { movePage } from "@/actions/pages";` (`deletePage`와 같은 줄로 합쳐도 됨), `import { buildTree, flattenTree, selfAndDescendantIds } from "@/lib/page-tree";`
2. 시그니처에 searchParams 추가:

```tsx
export default async function PageView({
  params,
  searchParams,
}: {
  params: Promise<{ spaceKey: string; slug: string }>;
  searchParams: Promise<{ error?: string }>;
}) {
  const { spaceKey, slug: rawSlug } = await params;
  const { error } = await searchParams;
```

3. `backlinks` 조회 근처(page가 존재하는 분기)에 이동 대상 데이터 추가:

```tsx
  // 이동 드롭다운: 자기 자신·자손 제외한 전체 문서(트리 순서·들여쓰기)
  const allPages = canEdit
    ? await prisma.page.findMany({
        where: { spaceId: space.id },
        select: { id: true, slug: true, title: true, parentId: true },
      })
    : [];
  const blocked = canEdit ? selfAndDescendantIds(allPages, page.id) : new Set<string>();
  const moveTargets = canEdit
    ? flattenTree(buildTree(allPages)).filter((t) => !blocked.has(t.id))
    : [];
```

4. `<main className="py-10">` 바로 다음에 에러 배너:

```tsx
      {error && (
        <div className="notice notice-warn mb-4" role="alert">
          {error}
        </div>
      )}
```

5. canEdit 버튼 그룹(`<div className="flex shrink-0 gap-2">`) 안, 삭제 form 앞에 이동 폼:

```tsx
            <form action={movePage.bind(null, spaceKey, slug)} className="flex items-center gap-1">
              <select name="parent" defaultValue={page.parentId ?? ""} className="select w-auto" aria-label="이동할 위치">
                <option value="">(최상위)</option>
                {moveTargets.map((t) => (
                  <option key={t.id} value={t.slug}>
                    {"  ".repeat(t.depth) + t.title}
                  </option>
                ))}
              </select>
              <button className="btn btn-ghost btn-sm">이동</button>
            </form>
```

주의: `defaultValue={page.parentId ?? ""}`는 option value가 slug라 id와 매칭되지 않는다 — **현재 부모의 slug**로 맞춘다: 데이터 준비에서 `const currentParentSlug = allPages.find((p) => p.id === page.parentId)?.slug ?? "";`를 만들어 `defaultValue={currentParentSlug}` 사용.

- [ ] **Step 4: 검증 + Commit**

```bash
npm test && npx tsc --noEmit
git add src/lib/pages.ts src/actions/pages.ts "src/app/s/[spaceKey]/[slug]/page.tsx"
git commit -m "feat(tree): 문서 이동(순환 방지)과 삭제 시 자식 승격

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: 사이드바 트리

**Files:**
- Modify: `src/app/s/[spaceKey]/layout.tsx` (조회 확장)
- Modify: `src/components/SidebarDocs.tsx` (트리 렌더)
- Modify: `src/components/Sidebar.tsx` (pages prop 타입 전달 — 확인 후 타입만)

**Interfaces:**
- Consumes: Task 1의 `buildTree`, `TreeNode` (클라이언트에서 import — 순수 모듈).
- Produces: e2e 계약 — 사이드바에서 자식 문서가 들여쓰기(`padding-left`)로 표시, 접기 토글 `aria-label="접기"/"펼치기"`.

- [ ] **Step 1: layout 조회 확장**

`src/app/s/[spaceKey]/layout.tsx`의 페이지 조회를:

```tsx
    prisma.page.findMany({
      where: { spaceId: space.id },
      orderBy: { title: "asc" },
      select: { id: true, slug: true, title: true, parentId: true },
    }),
```

- [ ] **Step 2: SidebarDocs 트리 렌더**

`src/components/SidebarDocs.tsx` 전체 교체:

```tsx
"use client";

import { useState } from "react";
import Link from "next/link";
import { NavLink } from "@/components/NavLink";
import { buildTree, type TreeNode, type TreePageInput } from "@/lib/page-tree";

interface Props {
  currentKey: string;
  currentName: string;
  pages: TreePageInput[];
  canEdit: boolean;
  canManage: boolean;
}

function TreeItem({
  node,
  currentKey,
  depth,
  collapsed,
  onToggle,
}: {
  node: TreeNode;
  currentKey: string;
  depth: number;
  collapsed: Set<string>;
  onToggle: (id: string) => void;
}) {
  const hasChildren = node.children.length > 0;
  const isCollapsed = collapsed.has(node.id);
  return (
    <div>
      <div className="flex items-center" style={{ paddingLeft: depth * 12 }}>
        {hasChildren ? (
          <button
            type="button"
            aria-label={isCollapsed ? "펼치기" : "접기"}
            onClick={() => onToggle(node.id)}
            className="lnb__twisty"
          >
            {isCollapsed ? "▸" : "▾"}
          </button>
        ) : (
          <span className="lnb__twisty" aria-hidden="true" />
        )}
        <NavLink href={`/s/${currentKey}/${encodeURIComponent(node.slug)}`}>
          <span className="lnb__name">{node.title}</span>
        </NavLink>
      </div>
      {hasChildren && !isCollapsed && (
        <div>
          {node.children.map((c) => (
            <TreeItem key={c.id} node={c} currentKey={currentKey} depth={depth + 1} collapsed={collapsed} onToggle={onToggle} />
          ))}
        </div>
      )}
    </div>
  );
}

export function SidebarDocs({ currentKey, currentName, pages, canEdit, canManage }: Props) {
  const [q, setQ] = useState("");
  const [collapsed, setCollapsed] = useState<Set<string>>(new Set());
  const query = q.trim().toLowerCase();
  const filtered = query ? pages.filter((p) => p.title.toLowerCase().includes(query)) : [];
  const tree = buildTree(pages);

  const toggle = (id: string) =>
    setCollapsed((prev) => {
      const next = new Set(prev);
      if (next.has(id)) next.delete(id);
      else next.add(id);
      return next;
    });

  return (
    <div className="lnb__docs">
      <div className="lnb__docs-head">
        <p className="lnb__label">{currentName} · 문서</p>
        {/* 문서가 많을 때만 필터 노출 */}
        {pages.length > 8 && (
          <input
            value={q}
            onChange={(e) => setQ(e.target.value)}
            placeholder="빠른 찾기"
            aria-label="빠른 찾기"
            className="lnb__filter"
          />
        )}
      </div>

      <div className="lnb__docs-list">
        <div className="lnb__list">
          {/* 필터 입력 중에는 플랫 매칭 목록, 비우면 트리 */}
          {query
            ? filtered.map((p) => (
                <NavLink key={p.id} href={`/s/${currentKey}/${encodeURIComponent(p.slug)}`}>
                  <span className="lnb__name">{p.title}</span>
                </NavLink>
              ))
            : tree.map((n) => (
                <TreeItem key={n.id} node={n} currentKey={currentKey} depth={0} collapsed={collapsed} onToggle={toggle} />
              ))}
        </div>
        {pages.length === 0 && <p className="lnb__empty">문서 없음</p>}
        {query !== "" && pages.length > 0 && filtered.length === 0 && (
          <p className="lnb__empty">일치하는 문서 없음</p>
        )}
      </div>

      <div className="lnb__bottom">
        {canEdit && (
          <Link href={`/s/${currentKey}/new`} className="lnb__add">
            + 새 문서
          </Link>
        )}
        {canManage && (
          <Link href={`/s/${currentKey}/settings`} className="lnb__add">
            스페이스 설정
          </Link>
        )}
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Sidebar.tsx prop 타입 확인**

`src/components/Sidebar.tsx`가 `pages`를 그대로 `SidebarDocs`에 전달하는지 확인하고, prop 타입이 `{slug,title}[]`로 좁혀져 있으면 `TreePageInput[]`으로 갱신 (전달만 하면 타입 전파로 충분할 수 있음 — tsc가 알려줌).

- [ ] **Step 4: 트위스티 최소 스타일**

`src/app/globals.css`의 lnb 관련 블록 근처에 추가:

```css
.lnb__twisty {
  width: 16px;
  flex-shrink: 0;
  background: none;
  border: none;
  padding: 0;
  color: var(--muted, #888);
  font-size: 10px;
  cursor: pointer;
  text-align: center;
}
```

(Files에 `src/app/globals.css` 추가 — Modify.)

- [ ] **Step 5: 검증 + Commit**

```bash
npm test && npx tsc --noEmit
git add "src/app/s/[spaceKey]/layout.tsx" src/components/SidebarDocs.tsx src/components/Sidebar.tsx src/app/globals.css
git commit -m "feat(tree): 사이드바 트리 렌더 — 들여쓰기·접기/펼치기, 필터 중 플랫

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: API + MCP

**Files:**
- Modify: `src/app/api/spaces/[spaceKey]/pages/route.ts` (GET parentSlug, POST parent)
- Create: `src/app/api/spaces/[spaceKey]/pages/[slug]/move/route.ts`
- Modify: `src/lib/pages.ts` (`deletePageInSpace` 공용 헬퍼 신규)
- Modify: `src/actions/pages.ts` (`deletePage`가 헬퍼 사용)
- Modify: `src/app/api/spaces/[spaceKey]/pages/[slug]/route.ts` (DELETE가 헬퍼 사용 — **Task 3 리뷰 Important: API 삭제가 자식 승격 없이 고아화하던 불일치 해소**)
- Modify: `mcp-server/src/tools.ts` (list_pages 트리 표기, create_page parent, move_page 신규, SERVER_INSTRUCTIONS)
- Modify: `mcp-server/package.json` (버전 bump)

**Interfaces:**
- Consumes: Task 2의 `createPageInSpace.parentId`, Task 3의 `movePageInSpace`.
- Produces: `GET pages` 응답 항목에 `parentSlug: string | null`. `POST .../move` body `{ parent: string | null }` → 200 / 404 / 422. MCP `move_page(space, slug, parent)`. `deletePageInSpace({spaceId, slug}) → { found: boolean }` — 승격 트랜잭션+캐시 무효화를 내장한 유일한 삭제 경로.

- [ ] **Step 0: 공용 삭제 헬퍼 — 웹/API 시맨틱 통일 (Task 3 리뷰 Important 해소)**

`src/lib/pages.ts`에 추가 (import에 `invalidatePageCache`는 이미 있음):

```ts
/**
 * 페이지 삭제 — 자식은 삭제 문서의 부모로 승격한다(트리 내용 보존).
 * 웹 액션과 REST DELETE가 공유하는 유일한 삭제 경로.
 */
export async function deletePageInSpace(input: { spaceId: string; slug: string }): Promise<{ found: boolean }> {
  const page = await prisma.page.findUnique({
    where: { spaceId_slug: { spaceId: input.spaceId, slug: input.slug } },
    select: { id: true, parentId: true },
  });
  if (!page) return { found: false };
  await prisma.$transaction([
    prisma.page.updateMany({ where: { parentId: page.id }, data: { parentId: page.parentId } }),
    prisma.page.delete({ where: { id: page.id } }),
  ]);
  invalidatePageCache(page.id);
  return { found: true };
}
```

`src/actions/pages.ts`의 `deletePage` 본문을 헬퍼 호출로 교체 (동작 동일 — 중복 제거):

```ts
export async function deletePage(spaceKey: string, slug: string) {
  const { space } = await requireSpaceRole(spaceKey, "editor");
  await deletePageInSpace({ spaceId: space.id, slug });
  revalidatePath(`/s/${spaceKey}`);
  redirect(`/s/${spaceKey}`);
}
```

`src/app/api/spaces/[spaceKey]/pages/[slug]/route.ts`의 DELETE 핸들러에서 `prisma.page.deleteMany(...)`(및 주변의 수동 캐시 무효화가 있다면 그것까지)를 다음으로 교체 — 기존 404/응답 형태는 유지:

```ts
  const result = await deletePageInSpace({ spaceId: auth.space.id, slug });
  if (!result.found) return Response.json({ error: "없습니다." }, { status: 404 });
```

(실제 DELETE 핸들러의 현재 형태를 확인해 응답 규약을 보존할 것 — 404 은닉 규칙 유지.)

- [ ] **Step 1: GET — parentSlug 포함**

`route.ts`의 GET 조회·응답을 교체:

```ts
  const pages = await prisma.page.findMany({
    where: { spaceId: auth.space.id },
    orderBy: { title: "asc" },
    select: { slug: true, title: true, updatedAt: true, parent: { select: { slug: true } } },
  });
  return Response.json({
    space: { key: auth.space.key, name: auth.space.name },
    pages: pages.map((p) => ({
      slug: p.slug,
      title: p.title,
      updatedAt: p.updatedAt,
      parentSlug: p.parent?.slug ?? null,
    })),
  });
```

- [ ] **Step 2: POST — parent 옵션**

body 파싱에 `parent?: unknown` 추가 후, `createPageInSpace` 호출 전에:

```ts
  const parentSlug = typeof body.parent === "string" && body.parent !== "" ? body.parent : null;
  let parentId: string | null = null;
  if (parentSlug) {
    const parent = await prisma.page.findUnique({
      where: { spaceId_slug: { spaceId: auth.space.id, slug: parentSlug } },
      select: { id: true },
    });
    if (!parent) {
      return Response.json({ error: "부모 문서가 없습니다." }, { status: 422 });
    }
    parentId = parent.id;
  }
```

그리고 `createPageInSpace({ ... })`에 `parentId,` 추가.

- [ ] **Step 3: move 라우트 신규**

`src/app/api/spaces/[spaceKey]/pages/[slug]/move/route.ts` 생성:

```ts
import { NextRequest } from "next/server";
import { requireApiSpaceRole } from "@/lib/api-auth";
import { movePageInSpace } from "@/lib/pages";

// POST /api/spaces/{spaceKey}/pages/{slug}/move — 트리 이동 (editor 권한)
// body: { parent: string | null }  — parent는 부모 slug, null이면 최상위.
export async function POST(
  req: NextRequest,
  ctx: { params: Promise<{ spaceKey: string; slug: string }> },
) {
  const { spaceKey, slug: rawSlug } = await ctx.params;
  const slug = decodeURIComponent(rawSlug);
  const auth = await requireApiSpaceRole(req, spaceKey, "editor");
  if (!auth.ok) return auth.response;

  let body: { parent?: unknown };
  try {
    body = await req.json();
  } catch {
    return Response.json({ error: "JSON 본문이 올바르지 않습니다." }, { status: 400 });
  }
  const parentSlug =
    body.parent === null || body.parent === undefined || body.parent === ""
      ? null
      : typeof body.parent === "string"
        ? body.parent
        : undefined;
  if (parentSlug === undefined) {
    return Response.json({ error: "parent는 문자열 slug 또는 null이어야 합니다." }, { status: 400 });
  }

  const result = await movePageInSpace({ spaceId: auth.space.id, slug, parentSlug });
  if (!result.ok) {
    if (result.reason === "not-found") return Response.json({ error: "없습니다." }, { status: 404 });
    const msg = result.reason === "cycle" ? "자기 자신이나 하위 문서로는 이동할 수 없습니다." : "부모 문서가 없습니다.";
    return Response.json({ error: msg }, { status: 422 });
  }
  return Response.json({ moved: true, slug, parent: parentSlug });
}
```

- [ ] **Step 4: MCP 도구**

`mcp-server/src/tools.ts`:

1. `list_pages` 핸들러를 트리 들여쓰기 텍스트로 교체 (mcp-server는 앱 lib을 import할 수 없어 소형 트리 구성을 내장):

```ts
  server.registerTool(
    "list_pages",
    {
      title: "페이지 목록",
      description:
        "특정 스페이스의 페이지 트리를 반환합니다. 들여쓰기가 부모-자식 관계이며, 각 항목에 slug와 parentSlug가 있습니다.",
      inputSchema: { space: z.string().describe("스페이스 키 (예: eng). list_spaces의 key 값)") },
    },
    async ({ space }) => {
      const r = await api(`/api/spaces/${encodeURIComponent(space)}/pages`);
      if (!r.ok) return toResult(r);
      const data = r.data as {
        space: unknown;
        pages: { slug: string; title: string; parentSlug: string | null }[];
      };
      // 들여쓰기 트리 텍스트 구성 (형제는 API 순서 = 제목순)
      const children = new Map<string | null, typeof data.pages>();
      for (const p of data.pages) {
        const key = p.parentSlug ?? null;
        const arr = children.get(key) ?? [];
        arr.push(p);
        children.set(key, arr);
      }
      const lines: string[] = [];
      const walk = (parent: string | null, depth: number) => {
        for (const p of children.get(parent) ?? []) {
          lines.push(`${"  ".repeat(depth)}- ${p.title} (slug: ${p.slug})`);
          walk(p.slug, depth + 1);
        }
      };
      walk(null, 0);
      return { content: [{ type: "text" as const, text: JSON.stringify(data.space) + "\n" + lines.join("\n") }] };
    },
  );
```

2. `create_page` inputSchema에 추가:

```ts
        parent: z.string().optional().describe("부모 페이지 slug(생략 시 최상위). 페이지 트리의 위치를 정합니다."),
```

핸들러의 POST body에 `parent`를 그대로 전달.

3. `move_page` 신규 등록 (delete_page 근처):

```ts
  server.registerTool(
    "move_page",
    {
      title: "페이지 이동",
      description:
        "페이지를 트리에서 이동합니다(editor 권한). parent에 부모 slug를 주거나, 최상위로 옮기려면 생략합니다. 자기 자신/하위로는 이동할 수 없습니다(422).",
      inputSchema: {
        space: z.string().describe("스페이스 키"),
        slug: z.string().describe("이동할 페이지 slug"),
        parent: z.string().optional().describe("부모 페이지 slug(생략 시 최상위)"),
      },
    },
    async ({ space, slug, parent }) =>
      toResult(
        await api(
          `/api/spaces/${encodeURIComponent(space)}/pages/${encodeURIComponent(slug)}/move`,
          { method: "POST", body: JSON.stringify({ parent: parent ?? null }) },
        ),
      ),
  );
```

(`api` 헬퍼의 실제 시그니처를 확인해 맞출 것 — 기존 create_page의 POST 호출 방식을 따른다.)

4. SERVER_INSTRUCTIONS의 "규칙:" 섹션 앞에 추가:

```
트리:
- 문서는 스페이스 안에서 부모-자식 트리를 이룹니다. list_pages의 들여쓰기가 구조입니다.
- 새 문서를 만들 때 관련 상위 문서가 있으면 create_page의 parent로 그 slug를 지정하세요.
- move_page로 위치를 바꿀 수 있습니다. 이동해도 slug/링크는 변하지 않습니다.
```

5. `mcp-server/package.json` version을 한 단계 올린다 (예: 1.2.0 → 1.3.0).

- [ ] **Step 5: 검증 + Commit**

```bash
npm test && npx tsc --noEmit
cd mcp-server && npm run build && cd ..   # mcp-server 빌드는 앱 .next와 무관 — 허용
git add "src/app/api/spaces/[spaceKey]/pages" mcp-server/src/tools.ts mcp-server/package.json
git commit -m "feat(tree): API parentSlug/move 라우트 + MCP 트리 표기·move_page

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6: e2e + MCP 스모크 + 전체 검증 + PR

**Files:**
- Modify: `e2e/wiki.spec.ts` (신규 테스트 1건 — 파일 끝)
- Modify: `mcp-server/smoke-test.mjs` (트리 시나리오 추가)

**Interfaces:**
- Consumes: Task 2-5의 UI/API/MCP 계약 전부.

- [ ] **Step 1: e2e — 트리 시나리오 (파일 끝 추가)**

```ts
test("페이지 트리: 하위 문서 생성 → 사이드바 트리 → 이동 → 삭제 승격", async ({ page }) => {
  await login(page, "alice", "alice1234");

  // 부모 문서 생성
  await page.goto("/s/eng/new");
  await page.locator('input[name="title"]').fill("트리 부모");
  await page.getByRole("button", { name: "저장" }).click();
  await expect(page.getByRole("heading", { name: "트리 부모" })).toBeVisible();

  // "하위 문서" 버튼으로 자식 생성
  await page.getByRole("link", { name: "하위 문서" }).click();
  await expect(page.getByText("'트리 부모' 하위에 만듭니다.")).toBeVisible();
  await page.locator('input[name="title"]').fill("트리 자식");
  await page.getByRole("button", { name: "저장" }).click();
  await expect(page.getByRole("heading", { name: "트리 자식" })).toBeVisible();

  // 사이드바에서 자식이 부모 아래 들여쓰기로 보인다 (접기 토글 존재로 부모 확인)
  const sidebar = page.locator(".lnb__docs");
  await expect(sidebar.getByRole("button", { name: "접기" }).first()).toBeVisible();
  await expect(sidebar.getByRole("link", { name: "트리 자식" })).toBeVisible();

  // 자식을 최상위로 이동
  const moveForm = page.locator('form:has(select[name="parent"])');
  await moveForm.locator('select[name="parent"]').selectOption({ label: "(최상위)" });
  await moveForm.getByRole("button", { name: "이동" }).click();
  await expect(page.getByRole("heading", { name: "트리 자식" })).toBeVisible();

  // 순환 방지: 부모를 자식(현재 최상위) 아래로 옮긴 뒤, 자식을 다시 부모 아래로 → 정상 (단순 이동 확인)
  // 삭제 승격: 부모를 다시 만들어 자식을 그 아래로 옮기고, 부모 삭제 → 자식은 최상위로 승격되어 살아있다
  await page.goto("/s/eng/트리-부모");
  await page.locator('form:has(select[name="parent"])').locator('select[name="parent"]').selectOption({ label: "트리 자식" });
  await page.locator('form:has(select[name="parent"])').getByRole("button", { name: "이동" }).click();
  await page.once("dialog", (d) => d.accept());
  await page.locator('form:has(select[name="parent"])').locator("..").locator('button:has-text("삭제")').click();
  // 부모("트리 부모", 이제 "트리 자식"의 자식) 삭제 후에도 트리 자식은 살아있고 최상위 구조 유지
  await page.goto("/s/eng/트리-자식");
  await expect(page.getByRole("heading", { name: "트리 자식" })).toBeVisible();
});
```

(정확한 셀렉터는 구현 DOM에 맞춰 조정 가능 — 시나리오 의미를 유지할 것: 하위 생성 → 트리 표시 → 이동 → 삭제 후 자식 생존. 삭제 confirm은 `page.once("dialog", accept)` 패턴.)

- [ ] **Step 2: MCP 스모크 — 트리 라운드트립 추가**

`mcp-server/smoke-test.mjs`의 delete_page 정리 전에 추가:

```js
const parentPage = await call("create_page", { space: "eng", title: "MCP 트리 부모", content: "부모" });
const parentSlug = JSON.parse(parentPage.text).slug;
const childPage = await call("create_page", { space: "eng", title: "MCP 트리 자식", content: "자식", parent: parentSlug });
const childSlug = JSON.parse(childPage.text).slug;
const treeList = await call("list_pages", { space: "eng" });
console.log("\n[list_pages tree] isError=%s\n%s", treeList.isError, treeList.text.slice(0, 400));
const moved = await call("move_page", { space: "eng", slug: childSlug });
console.log("\n[move_page → 최상위] isError=%s\n%s", moved.isError, moved.text);
const cycle = await call("move_page", { space: "eng", slug: parentSlug, parent: parentSlug });
console.log("\n[move_page 순환] isError=%s (true 기대)\n%s", cycle.isError, cycle.text.slice(0, 120));
await call("delete_page", { space: "eng", slug: childSlug });
await call("delete_page", { space: "eng", slug: parentSlug });
```

- [ ] **Step 3: 단위 + 타입**

```bash
npm test && npx tsc --noEmit
```

- [ ] **Step 4: e2e 준비 — DB 리셋 + dev 서버 재기동 (컨트롤러 담당 — 시도 금지)**

- [ ] **Step 5: e2e 전체**

```bash
npm run e2e
```

Expected: **13/13** PASS.

- [ ] **Step 6: MCP 스모크 실행**

임시 PAT 필요 — tsx 원라이너로 발급(구 플랜 선례):

```bash
npx tsx -e "
import { prisma } from './src/lib/db';
import { generateToken } from './src/lib/api-auth';
(async () => {
  const alice = await prisma.user.findFirst({ where: { email: 'alice@example.com' } });
  if (!alice) throw new Error('alice가 로그인한 적 없음 — e2e를 먼저 실행');
  const t = generateToken();
  await prisma.apiToken.create({ data: { userId: alice.id, name: 'smoke', tokenHash: t.hash, prefix: t.prefix } });
  console.log(t.raw);
  process.exit(0);
})();
"
# 출력된 토큰으로:
cd mcp-server && npm run build && WIKI_API_TOKEN=<토큰> node smoke-test.mjs && cd ..
```

Expected: 트리 시나리오 포함 전 단계 정상, 순환 move는 isError=true. 종료 후 DB에서 smoke 토큰 삭제(`prisma.apiToken.deleteMany({ where: { name: 'smoke' } })` — tsx 원라이너).

- [ ] **Step 7: 커밋 + PR**

```bash
git add e2e/wiki.spec.ts mcp-server/smoke-test.mjs
git commit -m "test(tree): 페이지 트리 e2e + MCP 스모크 시나리오

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
git push -u origin feat/page-tree
gh pr create --title "feat: 페이지 트리 — 스페이스 내 문서 계층" --body "$(cat <<'EOF'
## Summary
- `Page.parentId` self-relation — 폴더 엔티티 없이 모든 노드가 문서인 페이지 트리 (Confluence/Notion 방식)
- 하위 문서 생성(문서 화면 버튼), 이동(순환 방지, `?error=` 배너), 삭제 시 자식은 조부모로 승격(내용 보존)
- 사이드바 트리(들여쓰기·접기/펼치기, 필터 중 플랫), slug/URL/위키링크는 트리와 독립(이동해도 불변)
- API: pages 목록 `parentSlug`, 생성 `parent`, 신규 `POST .../move`. MCP: `list_pages` 트리 표기, `create_page` parent, 신규 `move_page`

설계: docs/superpowers/specs/2026-07-17-page-tree-design.md

## Test plan
- [ ] unit 57/57 (page-tree 순수 로직 포함) / tsc 0
- [ ] e2e 13/13 — 하위 생성→트리 표시→이동→삭제 승격 시나리오 포함
- [ ] MCP 스모크 — create(parent)/list(트리)/move/순환 거부 라운드트립

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

---

## Self-Review 결과

- **스펙 커버리지:** 데이터 모델·정렬·고아 처리(Task 1), 생성 UX(Task 2), 이동·순환·삭제 승격·배너(Task 3), 사이드바 트리·필터 플랫(Task 4), API/MCP(Task 5), 테스트 3층(Task 6). breadcrumb 등 범위 밖 항목 미포함 확인.
- **타입 일관성:** `TreePageInput`/`TreeNode`/`flattenTree`(Task 1 → 3·4·5), `movePageInSpace` 반환 유니언(Task 3 → 5), `createPage(spaceKey, parentSlug, formData)` 시그니처(Task 2 — 호출처 new 페이지 1곳 동시 수정), e2e 셀렉터 계약(하위 문서/이동/접기 — Task 2·3·4 → 6).
- **주의점 명시:** 이동 드롭다운 defaultValue는 slug 기준(id 아님 — Task 3 Step 3에 교정 노트), mcp-server는 앱 lib import 불가라 트리 구성 소형 중복 허용, e2e 셀렉터는 구현 DOM 기준 조정 허용(시나리오 의미 고정).
