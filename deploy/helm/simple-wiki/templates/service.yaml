apiVersion: v1
kind: Service
metadata:
  name: {{ include "simple-wiki.appServiceName" . }}
  labels:
    {{- include "simple-wiki.labels" . | nindent 4 }}
    app.kubernetes.io/component: app
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "simple-wiki.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: app
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
