{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "simple-wiki.serviceAccountName" . }}
  labels:
    {{- include "simple-wiki.labels" . | nindent 4 }}
{{- end }}
